import { HighSalary } from './high-salary';

describe('HighSalary', () => {
  it('should create an instance', () => {
    const directive = new HighSalary();
    expect(directive).toBeTruthy();
  });
});
